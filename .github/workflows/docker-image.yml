name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
#       run: docker image build -t marcinchamera/zadanie1-image:v1
#       run: docker build . --file Dockerfile --tag marcinchamera/zadanie1-image:v1
#       run: docker build -f Dockerfile -t marcinchamera/zadanie1-image:v1 .
      run: docker build zadanie1/Dockerfile -t marcinchamera/zadanie1-image:v1
